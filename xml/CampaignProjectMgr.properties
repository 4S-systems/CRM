saveCampaignProjects=INSERT INTO CAMPAIGN_PROJECT VALUES (?,?,?,?,SYSDATE)
getProjectsByCampaign=SELECT cp.PROJECT_ID, p.PROJECT_NAME FROM CAMPAIGN_PROJECT cp left join PROJECT p on cp.PROJECT_ID = p.PROJECT_ID WHERE CAMPAIGN_ID = ? OR CAMPAIGN_ID IN (SELECT PARENT_ID FROM CAMPAIGN WHERE ID = ?)