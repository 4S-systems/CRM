getSLANotification=SELECT ID, BUSINESS_ID, BUSINESS_ID_BY_DATE, CLIENT_NAME, ROUND((TIME_REMAINING_MINUTES / 60), 1) TIME_REMAINING_MINUTES FROM SLA_NOTIFICATION WHERE CREATED_BY = ? AND OPTION6 != '1' AND TIME_REMAINING_MINUTES BETWEEN 0 AND ? ORDER BY TIME_REMAINING_MINUTES
